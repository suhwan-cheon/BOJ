#include <iostream>
#include <vector>
using namespace std;
int n, k;
int a[100001];
int ans = 0;
void go(vector<int>& c) {
	if (c.size() == k - 1) {
		
	}
	for (int i = 0; i < n; i++) {

	}
}
int main()
{
	scanf("%d %d", &n, &k);
	for (int i = 0; i < n; i++) {
		scanf("%d", &a[i]);
	}
	vector<int> c;
	go(c);
}

